[package]
name = "jot"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
clap = { version = "4.6.6", features = ["derive", "string"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
